# Screenshots

Drop the three PNGs captured during playtest here:

- `splash.png` — the inline splash with the mini orb + "Open Watchtower" button
- `watchtower.png` — full Watchtower view with status orb + live anomaly feed
- `drilldown.png` — drill-down modal with entities + bulk-action confirmation step

Recommended: 1080×720 or 1280×720, PNG, <500 KB each.
